import TitleBar from "../components/TitleBar";
import MapContainer from "../components/MapContainer";
const Home = () => (
    <div className = "Home">
    <TitleBar />
    < MapContainer />
    </div>
);


export default Home;
