import TitleBar  from "../components/TitleBar";
import Accordion from "../components/ResourceList"
const Test = () => (
    <div>
    <TitleBar />
    <Accordion>

    </Accordion>
    </div>
);

export default Test;
